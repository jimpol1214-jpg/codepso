---
const phoneNumber = "51999999999"; // 🔹 Cambia por tu número real (sin +)
const message = encodeURIComponent("Hola, deseo más información sobre sus servicios."); 
---

---
---

---
---

<style>
.whatsapp-container {
  position: fixed;
  bottom: 20px;
  right: 20px;
  display: flex;
  align-items: center;
  gap: 10px;
  z-index: 1000;
}

.whatsapp-message {
  background: #f1f1f1;
  color: #333;
  padding: 8px 10px;
  border-radius: 8px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
  font-size: 13px;
  font-weight: 500;
  max-width: 180px;
  text-align: center;
  line-height: 1.3;
  animation: fadeIn 1s ease-in-out;
  transition: opacity 1s ease-in-out;
}

.whatsapp-message.hide {
  opacity: 0;
  pointer-events: none;
}

.whatsapp-button {
  background-color: #25d366;
  color: white;
  border-radius: 50%;
  width: 55px;
  height: 55px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
  animation: bounce 2s infinite;
  cursor: pointer;
  transition: transform 0.3s;
}

.whatsapp-button:hover {
  transform: scale(1.1);
}

/* Animaciones */
@keyframes bounce {
  0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
  40% { transform: translateY(-10px); }
  60% { transform: translateY(-5px); }
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateX(10px); }
  to { opacity: 1; transform: translateX(0); }
}

/* Ocultar automáticamente en móvil */
@media (max-width: 768px) {
  .whatsapp-message {
    animation: fadeIn 1s ease-in-out, fadeOut 1s ease-in-out 5s forwards;
  }

  @keyframes fadeOut {
    to { opacity: 0; transform: translateX(10px); }
  }
}
</style>

<div class="whatsapp-container">
  <div class="whatsapp-message" id="whatsappMessage">
    💬 ¿En qué podemos ayudarte?<br />
    Escríbenos ahora
  </div>

  <a
    href="https://wa.me/51999999999"
    class="whatsapp-button"
    target="_blank"
    rel="noopener noreferrer"
  >
    <i class="fab fa-whatsapp"></i>
  </a>
</div>




